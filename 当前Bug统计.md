# 当前 Bug 统计

> 最后更新：2026-02-24 (V3 归档，进入 V4 准备期)

- 文件列表只在打开文件夹时加载。在此之后从文件资源管理器添加文件，或进行其他操作，软件内无响应；
- 如果其他软件更改了文档，Writer需要刷新文档（待详细讨论实现方案和边界）
- 需要支持多选文字时，统一设置这些文字的字号，加醋，斜体等等全部样式

## 遗留 BUG 与 待攻坚项 (已转入 V4 跟踪)

所有 V3 未决的缺陷（BUG-01, BUG-06, BUG-07）以及 Tauri 补测阻塞项，已统一汇总至：
- `docs/current/V3遗留与V4待办.md`

## 最近已修复（V3 Sprint3 & Sprint4 查漏补缺）

以下 Bug 已在 V3 阶段彻底修复并归档：

- **Backspace 无法删除表格**：改用 `$pos.nodeBefore` API 修复。
- **表格内偶现填充字符 `&nbsp;`**：通过 `onUpdate` 规范化 `\xA0` 修复。
- **表头文本对齐不一致**：CSS 视觉修复。
- **空表格关闭重开后出现 `&nbsp;`**：Turndown 序列化正则剔除修复。
- **光标边界越界跳转**（多种场景）：通过 `handleKeyDown` 接管并修正选区。
- **标题字号切换功能回退**：补齐 `addKeyboardShortcuts` 映射修复。

## 待观测 (V4)

- 剪贴板深度清洗对常规 Markdown 粘贴的影响。
- 大文档场景下查找替换的内存压力。