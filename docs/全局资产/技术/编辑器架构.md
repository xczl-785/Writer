# 编辑器架构

**更新日期**: 2026-03-01

---

## 一、技术栈

- **编辑器核心**: TipTap 3（基于 ProseMirror）
- **扩展系统**: TipTap Extension API
- **Markdown 支持**: @tiptap/markdown

---

## 二、目录结构

```
src/ui/editor/
├── Editor.tsx              # 主编辑器组件
├── constants.ts            # 工具栏命令定义
├── components/             # 编辑器子组件
│   ├── EditorShell.tsx
│   ├── FindReplacePanel.tsx
│   ├── Toolbar.tsx
│   └── InsertTablePopover.tsx
├── extensions/             # TipTap 扩展
│   ├── index.ts
│   ├── toolbarShortcuts.ts
│   ├── findReplaceShortcuts.ts
│   └── keydownHandler.ts
├── menus/                  # 菜单组件
│   ├── SlashMenu.tsx
│   ├── BubbleMenu.tsx
│   └── GhostHint.tsx
├── handlers/               # 事件处理器
│   ├── menuCommandHandler.ts
│   └── contextMenuHandler.ts
├── hooks/                  # 编辑器专用 hooks
│   ├── useSafeCoords.ts
│   ├── useGhostHint.ts
│   └── useUndoRedo.ts
└── *.ts                    # 其他 hooks 和工具
```

---

## 三、TipTap 扩展清单

### 3.1 官方扩展

| 扩展                           | 用途                                                               |
| ------------------------------ | ------------------------------------------------------------------ |
| StarterKit                     | 基础文档结构（paragraph, heading, list, blockquote, codeBlock 等） |
| Table                          | 表格支持（可调整大小）                                             |
| TableRow/TableCell/TableHeader | 表格结构                                                           |
| Image                          | 图片支持（自定义 src 解析）                                        |

### 3.2 自定义扩展

| 扩展                         | 文件                                 | 用途                 |
| ---------------------------- | ------------------------------------ | -------------------- |
| BlockBoundaryExtension       | `ui/components/BlockBoundary/`       | 块级元素边界视觉提示 |
| ToolbarShortcutExtension     | `extensions/toolbarShortcuts.ts`     | 工具栏命令快捷键绑定 |
| FindReplaceShortcutExtension | `extensions/findReplaceShortcuts.ts` | 查找替换快捷键       |

---

## 四、编辑器架构图

```
┌─────────────────────────────────────────────────────────────┐
│                      Editor.tsx (主容器)                     │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌──────────────┐  ┌───────────────────┐  │
│  │ Toolbar     │  │ SlashMenu    │  │ BubbleMenu        │  │
│  │ (工具栏)    │  │ (斜杠菜单)   │  │ (选中浮动菜单)    │  │
│  └─────────────┘  └──────────────┘  └───────────────────┘  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │               useEditor (TipTap 核心)                 │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │  extensions: [                                        │  │
│  │    - StarterKit                                      │  │
│  │    - Table/TableRow/TableCell/TableHeader            │  │
│  │    - Image (自定义 src 解析)                         │  │
│  │    - BlockBoundaryExtension                          │  │
│  │    - ToolbarShortcutExtension                        │  │
│  │    - FindReplaceShortcutExtension                    │  │
│  │  ]                                                    │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                    Zustand 状态管理                          │
│  useEditorStore: { fileStates: Record<path, FileState> }   │
│  - content: string (Markdown 原文)                          │
│  - cursor: { line, column }                                 │
│  - isDirty: boolean                                         │
└─────────────────────────────────────────────────────────────┘
```

---

## 五、核心功能实现

### 5.1 工具栏

- 命令定义在 `constants.ts` 的 `TOOLBAR_COMMANDS` 数组
- `useToolbarCommands` hook 封装命令执行逻辑
- 按功能分组：Inline、Headings、Blocks

### 5.2 斜杠菜单

- 输入 `/` 触发（支持全角 `／`）
- 支持搜索过滤、键盘导航
- 命令分组：Basic Blocks、Advanced Components
- 实现：`menus/SlashMenu.tsx`

### 5.3 浮动菜单

- 选中文字时出现
- 80ms 防抖延迟
- 包含：B、I、S、</>、Link、Highlight 按钮
- 实现：`menus/BubbleMenu.tsx`

### 5.4 查找替换

- 快捷键：Cmd+F（查找）、Cmd+H（替换）
- 支持单个/全部替换、上下导航
- 实现：`useFindReplace.ts`

---

## 六、编辑器与状态同步

### 6.1 内容更新流程

```
用户输入 → TipTap Editor.onUpdate
    │
    ▼
MarkdownService.serialize(editor.getJSON())
    │
    ▼
updateFileContent(path, markdown)
    │
    ▼
setDirty(path, true)
    │
    ▼
AutosaveService.schedule(path, markdown)
```

### 6.2 文件加载流程

```
activeFile 变化 → useEffect 触发
    │
    ▼
MarkdownService.parse(content)
    │
    ▼
editor.commands.setContent(json)
```

---

## 七、扩展开发指南

### 7.1 创建新扩展

```typescript
// extensions/myExtension.ts
import { Extension } from '@tiptap/core';

export const MyExtension = Extension.create({
  name: 'my-extension',
  addKeyboardShortcuts() {
    return {
      'Mod-m': () => {
        // 执行操作
        return true;
      },
    };
  },
});
```

### 7.2 注册扩展

在 `Editor.tsx` 的 `extensions` 数组中添加：

```typescript
const extensions = useMemo(
  () => [
    // ... 其他扩展
    MyExtension,
  ],
  [],
);
```
