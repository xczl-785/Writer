# 服务层设计

**更新日期**: 2026-03-14

---

## 一、目录结构

```text
src/
├── domains/file/services/
│   ├── FsService.ts
│   ├── FsSafety.ts
│   ├── AutosaveService.ts
│   └── FileWatcherService.ts
├── domains/workspace/services/
│   ├── WorkspaceManager.ts
│   ├── WorkspaceStatePersistence.ts
│   ├── WorkspaceLockService.ts
│   ├── RecentItemsService.ts
│   ├── workspaceActions.ts
│   └── droppedPaths.ts
├── services/
│   ├── autosave/AutosaveService.ts   # 兼容 re-export
│   ├── error/ErrorService.ts
│   ├── images/ImageResolver.ts
│   ├── markdown/MarkdownService.ts
│   └── runtime/TauriWarmup.ts
└── 少量兼容入口：i18n/*、utils/*、services/autosave/*
```

---

## 二、服务职责清单

| 服务                | 职责                                | 后端通信          | 依赖                          |
| ------------------- | ----------------------------------- | ----------------- | ----------------------------- |
| **FsService**       | 文件/目录 CRUD、路径类型、编码检测  | `invoke`          | Tauri                         |
| **MarkdownService** | Markdown ⇔ EditorJSON 双向转换      | 无                | 无                            |
| **ImageResolver**   | 相对/绝对路径解析为 Tauri asset URL | `convertFileSrc`  | ErrorService                  |
| **AutosaveService** | 防抖自动保存、队列管理、重试        | FsService         | FsService, ErrorService       |
| **ErrorService**    | 错误分类、日志记录、UI 状态同步     | 无                | useStatusStore                |
| **FsSafety**        | 文件操作前脏文件 flush              | AutosaveService   | AutosaveService, ErrorService |
| **workspaceActions** | 工作区事务协调、打开/关闭/切换文件、回滚 | FsService / Workspace services | Workspace stores |
| **WorkspaceManager** | 工作区加载、拖拽接入、工作区文件编排 | FsService / workspaceActions | Workspace stores |
| **WorkspaceStatePersistence** | 最近状态持久化 | `read_json_file` / `write_json_file` | config commands |
| **TauriWarmup**     | Tauri 运行时桥接预热                | `invoke('greet')` | Tauri                         |

---

## 三、与 Tauri 后端通信

### 3.1 通信模式

所有后端通信集中在 `FsService.ts` 中，使用 `invoke` 调用：

```typescript
import { invoke } from '@tauri-apps/api/core';

// 读取文件
const content = await invoke('read_file', { path });

// 写入文件
await invoke('write_file_atomic', { path, content });

// 列出目录树
const tree = await invoke('list_tree', { path });
```

### 3.2 Tauri 命令清单

| 命令                     | 功能                        |
| ------------------------ | --------------------------- |
| `list_tree`              | 递归列出目录树              |
| `read_file`              | 读取文件内容                |
| `write_file_atomic`      | 原子写入（临时文件+重命名） |
| `create_file`            | 创建新文件                  |
| `create_dir`             | 创建新目录                  |
| `rename_node`            | 重命名/移动文件或目录       |
| `delete_node`            | 删除文件或目录              |
| `reveal_in_file_manager` | 在文件管理器中显示          |
| `save_image`             | 保存图片（二进制）          |
| `check_exists`           | 检查路径是否存在            |
| `get_path_kind`          | 获取路径类型                |
| `detect_file_encoding`   | 检测文件编码                |
| `parse_workspace_file`   | 解析工作区文件              |
| `save_workspace_file`    | 保存工作区文件              |
| `resolve_relative_path`  | 解析工作区内相对路径        |
| `read_json_file`         | 读取 JSON 配置文件          |
| `write_json_file`        | 原子写入 JSON 配置文件      |

---

## 四、服务依赖关系

```
                    ┌─────────────────────────────────────┐
                    │           Tauri Backend             │
                    └──────────────┬──────────────────────┘
                                   │
                    ┌──────────────▼──────────────────────┐
                    │          FsService                  │
                    │   (文件系统与树加载入口)             │
                    └──────────────┬──────────────────────┘
                                   │
            ┌──────────────────────┼──────────────────────┐
            │                      │                      │
┌───────────▼───────────┐  ┌───────▼───────┐  ┌───────────▼─────────┐
│    AutosaveService    │  │ ImageResolver │  │ workspaceActions    │
└───────────┬───────────┘  └───────┬───────┘  └───────────┬─────────┘
            │                      │
            │            ┌─────────▼─────────┐
            │            │   ErrorService    │
            │            └─────────┬─────────┘
            │                      │
┌───────────▼──────────────────────▼─────────────────────────────┐
│                        FsSafety                                 │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ WorkspaceManager / WorkspaceStatePersistence / RecentItemsService│
│          (围绕 workspaceActions 的领域服务协作)                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    MarkdownService                              │
│              (纯前端，无依赖，无后端通信)                         │
└─────────────────────────────────────────────────────────────────┘
```

---

## 五、关键服务详解

### 5.1 FsService

文件系统操作的统一入口，封装所有 Tauri `invoke` 调用。

**设计原则**：

- 单一入口：所有文件操作都通过 FsService
- 错误处理：统一返回类型，便于上层处理
- 类型安全：完整的 TypeScript 类型定义

### 5.2 AutosaveService

防抖自动保存调度器。

**核心机制**：

- 防抖延迟（默认配置）
- 待保存队列管理
- 重试机制
- 手动 flush 接口

### 5.3 MarkdownService

Markdown 与 TipTap JSON 双向转换。

**API**：

```typescript
// Markdown → TipTap JSON
const json = await MarkdownService.parse(markdown);

// TipTap JSON → Markdown
const markdown = await MarkdownService.serialize(json);
```

### 5.4 ImageResolver

解析图片相对/绝对路径为 Tauri asset URL。

**使用场景**：

- 编辑器中显示本地图片
- 导出时处理图片路径

---

## 六、设计模式

### 6.1 工具服务单例

所有服务使用对象字面量实现：

```typescript
export const FsService = {
  async readFile(path: string): Promise<string> {
    return invoke('read_file', { path });
  },
  // ...
};
```

### 6.2 依赖注入缺失

当前服务间仍以直接 import 为主，但领域边界已经明确：新代码优先依赖 `domains/*` 与 `shared/*`；兼容入口当前仅剩 `i18n/*`、`utils/*` 与 `services/autosave/*` 等少量历史路径。

**潜在改进**：

```typescript
// 可考虑引入依赖注入
type FsServiceInterface = {
  readFile: (path: string) => Promise<string>;
  // ...
};

export function createServices(fs: FsServiceInterface) {
  return {
    autosave: createAutosaveService(fs),
    // ...
  };
}
```
