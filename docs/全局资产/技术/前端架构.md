# 前端架构

**更新日期**: 2026-03-14

---

## 一、技术栈

- **框架**: React 19
- **状态管理**: Zustand 5
- **编辑器**: TipTap 3
- **样式**: Tailwind CSS 4
- **构建**: Vite 7

---

## 二、目录结构（领域驱动架构）

```
src/
├── app/                    # 应用入口层
│   ├── App.tsx             # 主应用组件
│   ├── commands/           # 菜单命令注册
│   ├── useNativeMenuBridge.ts
│   └── closeWorkflow.ts
├── domains/                # 领域层（核心业务）
│   ├── file/               # 文件领域
│   │   ├── services/       # FsService, FsSafety, AutosaveService, FileWatcherService
│   │   ├── state/          # fileStore (文件树状态)
│   │   └── ui/             # FileTreeNode, MultiRootFileTree
│   ├── editor/             # 编辑器领域
│   │   ├── core/           # Editor, EditorImpl, EditorOrchestrator
│   │   ├── domain/         # 纯逻辑 (findReplace, typewriter, slash, scroll)
│   │   ├── state/          # editorStore (多文档内容)
│   │   ├── hooks/          # 自定义 hooks
│   │   ├── ui/             # Toolbar, SlashMenu, BubbleMenu, FindReplacePanel
│   │   ├── integration/    # 桥接层 (contextBridge, persistenceBridge)
│   │   └── extensions/     # TipTap 扩展
│   ├── workspace/          # 工作区领域
│   │   ├── services/       # WorkspaceManager, workspaceActions, RecentItemsService
│   │   └── state/          # workspaceStore (工作区状态)
│   └── settings/           # 设置领域
│       ├── state/          # settingsStore (用户偏好)
│       └── ui/             # SettingsPanel
├── shared/                 # 共享层（跨领域复用）
│   ├── components/         # ContextMenu, Dialog, Outline
│   ├── i18n/               # 国际化
│   └── utils/              # 工具函数
├── services/               # 基础设施层（跨领域服务）
│   ├── error/              # ErrorService
│   ├── images/             # ImageResolver
│   ├── markdown/           # MarkdownService
│   └── runtime/            # TauriWarmup
├── state/                  # 全局状态层
│   ├── slices/             # statusSlice, viewModeSlice
│   └── actions/            # 跨 store 协调
├── ui/                     # UI 组件层（领域外 UI）
│   ├── sidebar/
│   ├── statusbar/
│   └── components/
└── main.tsx               # 入口
```

---

## 三、状态管理架构

### 3.1 架构模式

采用 **领域分层 + 多 Zustand Store 模式**：

- 每个领域维护自己的 store / service / UI 边界
- 应用级运行时状态保留在 `state/slices/`
- 跨领域协调通过 `state/actions/` 与 `domains/workspace/services/workspaceActions.ts` 实现
- `src/i18n/`、`src/utils/`、`src/state/slices/` 中保留兼容导出，供迁移期代码与测试复用

### 3.2 Store 职责

| Store               | 位置 | 职责           | 关键状态                                    |
| ------------------- | ---- | -------------- | ------------------------------------------- |
| `useEditorStore`    | domains/editor/state | 多文档内容管理 | `fileStates: Record<path, FileEditorState>` |
| `useFileTreeStore`  | domains/file/state | 文件树 UI 状态 | `rootFolders`, `expandedPaths`, `selectedPath` |
| `useWorkspaceStore` | domains/workspace/state | 工作区文件管理 | `folders`, `workspaceFile`, `openFiles`, `activeFile` |
| `useStatusStore`    | state/slices | 应用状态通知   | `status`, `saveStatus`, `saveError`         |
| `useSettingsStore`  | domains/settings/state | 用户偏好持久化 | `localePreference`, `typewriterEnabledByUser`, `focusZenEnabledByUser` |
| `useViewModeStore`  | state/slices | 运行时视图态   | `isZen`, `isFocusZen`, `isTypewriterActive` |

### 3.3 数据流

```
用户操作 → Component → Action Function → store.set() → State Update → Component Re-render
```

**示例：打开文件流程**

```
用户点击文件
    │
    ▼
workspaceActions.openFile(path)
    │
    ├── flushAffectedFiles(activeFile)  // 保存当前文件
    ├── FsService.readFile(path)        // 读取目标文件
    ├── useEditorStore.initializeFile() // 更新 editor store
    └── useWorkspaceStore.openFile()    // 更新 workspace store
    │
    ▼
订阅组件自动重新渲染
```

---

## 四、组件架构

### 4.1 组件层级

```
App
├── Sidebar
├── Editor
│   └── domains/editor/core/Editor
│       └── EditorOrchestrator → EditorImpl → EditorView → EditorShell
├── StatusBar
├── SettingsPanel
└── RecentWorkspacesMenu / EmptyStateWorkspace
```

### 4.2 组件通信模式

- **Props 传递**: 父子组件通信（主要模式）
- **自定义 Hook**: 逻辑复用（useOutlineExtractor, useContextMenu）
- **事件驱动**: 跨组件通信（editor.update → Outline 刷新）
- **注册中心**: ContextMenuRegistry（全局菜单管理）
- **Portal 渲染**: ContextMenu 脱离 DOM 层级

### 4.3 编辑区分层（领域驱动）

- `domains/editor/core/`: 布局模型与编排入口（`EditorLayoutModel`, `EditorOrchestrator`）
- `domains/editor/domain/`: 纯逻辑（find/typewriter/focus-zen 等）
- `domains/editor/integration/`: 菜单/右键/粘贴/持久化桥接
- `domains/editor/view/`: 渲染边界（`EditorView`）
- `domains/editor/ui/`: 具体 UI 组件（`EditorShell` 等）
- `shared/components/`: 跨领域复用组件（`ContextMenu`、`Dialog`、`Outline`）

---

## 五、关键设计决策

### 5.1 为什么选择领域驱动架构？

- **高内聚**: 相关代码组织在同一领域目录下
- **低耦合**: 领域间通过明确定义的接口通信
- **可维护性**: 新功能有明确的归属位置
- **可测试性**: 领域逻辑可独立测试

### 5.2 为什么选择 Zustand？

- 轻量级（比 Redux 简洁）
- 无需 Provider 包裹
- 支持在非组件环境使用（通过 `getState()`）
- TypeScript 友好

### 5.3 服务层设计原则

- 服务使用对象字面量实现（无实例化）
- 文件与工作区服务优先收敛到各自领域目录
- FsService 作为文件系统后端调用入口
- ErrorService 统一错误处理
