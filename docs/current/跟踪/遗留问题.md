# 遗留问题

> 用于记录后续待推进的需求项（非 Bug）。

## 当前遗留

1. 国际化（i18n）已完成核心模块

- 现状：已完成 P0+P1 模块国际化。
- 已覆盖：
  - 侧边栏 `Sidebar.tsx`
  - 状态栏 `StatusBar.tsx` + `statusBarUtils.ts`
  - 文件树右键菜单 `fileTreeMenu.tsx`
  - 编辑器右键菜单 `editorMenu.tsx`
  - 斜杠菜单 `SlashMenu.tsx`
  - 查找替换面板 `FindReplacePanel.tsx`
  - 插入表格弹窗 `InsertTablePopover.tsx`
  - 删除确认对话框 `ConfirmDialog.ts`
  - 错误消息服务 `ErrorService.ts`
  - 关闭工作流 `closeWorkflow.ts`
- 翻译键：新增约 150 个翻译键（中英文双语）
- 剩余工作：可添加语言切换 UI 入口（当前仅通过系统语言自动切换）

2. 文档加载性能待优化

- 当前打开文档时，对比其他同类软件，加载速度稍慢

3. 结构性感知待优化，参考 ./design-block-indicator-system.md
4.

5. 交互间距遗留联动（L-A-01 / G-04）收口跟踪（2026-03-06，已关闭）

- 现状：编辑区三间距（Top/Inline/Bottom）已验收并归档。
- 关闭结论：`L-A-01` 与 `G-04` 已在归档清单标记关闭。
- 归档位置：`docs/archive/2026-03-06_editor-spacing-closeout/`
- 替代说明：后续仅在归档目录维护三间距历史，不再在 `docs/current` 保留对应交互需求文档。

6. V5 交互测试遗留迁移（2026-03-06）

- 来源：`docs/archive/2026-03-06_v5-test-leftovers-closeout/V5交互测试遗留清单.md`（已迁移归档）。
- 遗留总览（迁移后以本文件为唯一维护入口）：
  - `L-ERR-01`：`Cannot update a component (App) while rendering a different component (App)`（阻断报错，待确认）
  - `L-C-01 / TC-C04`：Focus Zen 退出路径复杂，需规则收敛（待确认）
  - `L-D-01 / G-03`：Typewriter 45% 锚点稳定性回归（待验收）
  - `L-E-01 / L-E-04 / G-01`：Slash 上翻遮挡输入行（待确认）
  - `L-E-02 / L-E-03 / G-02`：Slash 键盘导航未驱动滚动跟随（待确认）
- 已关闭项：
  - `L-A-01 / G-04`：编辑区三间距问题已关闭并归档。
- 归档位置：`docs/archive/2026-03-06_v5-test-leftovers-closeout/V5交互测试遗留清单.md`
- Slash 专项当前入口：`docs/current/REQ-SLASH-MENU-遗留专项需求.md`（作为实现与验收基线，不依赖归档阅读）。

7. Typewriter 遗留联动（L-D-01 / G-03）收口跟踪（2026-03-06）

- 现状：Typewriter 已重做为 `45% + ±12px 死区 + rAF 合帧 + IME 组合态保护`，并接入显式顶部偏移变量 `--editor-content-offset-top`（Find-only）。
- 缺口：需完成 D 组与 H 组串联手工回归，确认观感与稳定性（尤其 `TC-D01`, `TC-D08`, `TC-D09`）。
- 关闭条件：
  - `TC-D01~TC-D10` 全量通过。
  - 串联用例 `TC-H02`, `TC-H04` 通过。
  - 本文件与 `docs/archive/2026-03-06_v5-interaction-closeout/交互/实施验收清单.md` 状态同步更新。
- 证据来源：
  - 自动化：`src/ui/editor/domain/typewriter/typewriterDomain.test.ts`、`src/ui/editor/hooks/useTypewriterAnchor.test.ts`、`src/ui/editor/hooks/useTypewriterAnchorBehavior.test.ts`
  - 验收清单：`docs/archive/2026-03-06_v5-interaction-closeout/交互/实施验收清单.md`

---

## 追加规范（手动维护）

- 新增条目请写明：`现状`、`缺口`、`建议后续`。
- 本文档聚焦“需求遗留”，不记录缺陷（Bug）。
