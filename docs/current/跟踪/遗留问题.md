# 遗留问题

> 用于记录后续待推进的需求项（非 Bug）。

## 当前遗留

1. 国际化（i18n）已完成核心模块

- 现状：已完成 P0+P1 模块国际化。
- 已覆盖：
  - 侧边栏 `Sidebar.tsx`
  - 状态栏 `StatusBar.tsx` + `statusBarUtils.ts`
  - 文件树右键菜单 `fileTreeMenu.tsx`
  - 编辑器右键菜单 `editorMenu.tsx`
  - 斜杠菜单 `SlashMenu.tsx`
  - 查找替换面板 `FindReplacePanel.tsx`
  - 插入表格弹窗 `InsertTablePopover.tsx`
  - 删除确认对话框 `ConfirmDialog.ts`
  - 错误消息服务 `ErrorService.ts`
  - 关闭工作流 `closeWorkflow.ts`
- 翻译键：新增约 150 个翻译键（中英文双语）
- 剩余工作：可添加语言切换 UI 入口（当前仅通过系统语言自动切换）

2. 文档加载性能待优化

- 当前打开文档时，对比其他同类软件，加载速度稍慢

3. 结构性感知待优化，参考 ./design-block-indicator-system.md
4.

5. 交互间距遗留联动（L-A-01 / G-04）收口跟踪（2026-03-06）

- 现状：编辑区三间距（Top/Inline/Bottom）已收敛到统一布局模型，底部 `40vh` 留白已修复“贴底”根因（移除 ProseMirror `h-full` 强制高度）。
- 缺口：仍需完成手工回归并补齐验收证据，方可将 `L-A-01` 与 `G-04` 由“待确认”改为“已关闭”。
- 关闭条件：
  - `TC-A04` / `TC-A05` / `TC-A06` / `TC-B03` / `TC-A08` / `TC-D10` 全量通过。
  - 回归记录包含窗口档位、操作步骤、观察结论。
  - `docs/current/交互/V5交互测试遗留清单.md` 中 `L-A-01` 与 `G-04` 状态同步更新。
- 证据来源：
  - 自动化：`src/ui/editor/core/EditorLayoutModel.test.ts`、`src/ui/editor/EditorStyles.test.ts`、`src/ui/editor/components/EditorShellLayoutBehavior.test.ts`、`src/ui/sidebar/SidebarResponsiveBehavior.test.ts`
  - 需求映射：`docs/current/交互/V5交互测试用例与需求映射.md`
  - 验收清单：`docs/current/交互/实施验收清单.md`

6. Typewriter 遗留联动（L-D-01 / G-03）收口跟踪（2026-03-06）

- 现状：Typewriter 已重做为 `45% + ±12px 死区 + rAF 合帧 + IME 组合态保护`，并接入显式顶部偏移变量 `--editor-content-offset-top`（Find-only）。
- 缺口：需完成 D 组与 H 组串联手工回归，确认观感与稳定性（尤其 `TC-D01`, `TC-D08`, `TC-D09`）。
- 关闭条件：
  - `TC-D01~TC-D10` 全量通过。
  - 串联用例 `TC-H02`, `TC-H04` 通过。
  - `docs/current/交互/V5交互测试遗留清单.md` 中 `L-D-01` 与 `G-03` 状态同步更新。
- 证据来源：
  - 自动化：`src/ui/editor/domain/typewriter/typewriterDomain.test.ts`、`src/ui/editor/hooks/useTypewriterAnchor.test.ts`、`src/ui/editor/hooks/useTypewriterAnchorBehavior.test.ts`
  - 验收清单：`docs/current/交互/实施验收清单.md`

---

## 追加规范（手动维护）

- 新增条目请写明：`现状`、`缺口`、`建议后续`。
- 本文档聚焦“需求遗留”，不记录缺陷（Bug）。
