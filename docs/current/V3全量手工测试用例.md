# V3 全量手工测试用例集

- **版本**：V3 (Sprint 1-3)
- **更新日期**：2026-02-22
- **测试环境**：Web (127.0.0.1:43173) / Tauri 桌面环境

---

## 0. 专项：P0 Bug 修复验证 (P0 Fixes)

| ID | 检查场景 | 操作步骤 | 预期结果 |
| :--- | :--- | :--- | :--- |
| **QA-P0-01** | **Backspace 删表** | 1. 插入 3x3 表格。<br>2. 在表格下方创建一个空行。<br>3. 在空行开头按 Backspace。 | **1.** 自动选中上方表格并提示“Table selected...”。<br>**2.** 冗余空行自动被清理。 |
| **QA-P0-02** | **删表两步确认** | 接上步，表格被选中（NodeSelection）状态下。 | 按 Backspace，表格立即被删除，左下角显示“Table deleted”。 |
| **QA-P0-03** | **非空段落防护** | 1. 插入表格，下方输入文本 "Hello"。<br>2. 光标放在 "H" 前按 Backspace。 | 仅选中表格，**不产生任何删除行为**（保护下方内容）。 |
| **QA-P0-04** | **`&nbsp;` 规范化** | 1. 从外部编辑器副本（如 VSCode/网页）拷贝一段带空格的文本直接粘贴到表格 Cell。<br>2. 保存文件并重新打开。 | 单元格内空格保持正常，**不再出现字面量 `&nbsp;` 文本**。 |

---

## 1. 编辑器破坏性操作 (FR-V3-01)

| ID | 检查场景 | 操作步骤 | 预期结果 |
| :--- | :--- | :--- | :--- |
| **QA-S1-01** | **统一提示规则** | 点击工具栏的“删除行”、“删除列”或“删除表格”。 | 状态栏必须显示具体的删除动作提示（如 "Row deleted"）。 |
| **QA-S1-02** | **撤销链路回归** | 执行删除操作（如删行）后，按 `Ctrl+Z`。 | 被删除的行/列/表必须无损恢复，且选区回到操作前位置。 |
| **QA-S1-03** | **连续撤销稳定性** | 连续执行：删行 -> 输入文字 -> 删表，然后连续按 `Ctrl+Z`。 | 编辑器状态应按历史记录逐级完美回滚，不出现 Crash 或选区错乱。 |

---

## 2. 表格高频操作与边界 (FR-V3-02)

| ID | 检查场景 | 操作步骤 | 预期结果 |
| :--- | :--- | :--- | :--- |
| **QA-S2-01** | **表格插入弹窗** | 点击工具栏 Tbl 图标，在弹窗输入 4x4 并确认。 | 1. 弹窗应自动聚焦 Row 输入框。<br>2. 插入的表格应为 4x4，含 Header。 |
| **QA-S2-02** | **跨块选区清理** | 跨过表格及其前后段落进行大范围选择并按 Backspace。 | 表格及其内容应被干净清除，不残留碎裂的 `<tr>` 标签。 |
| **QA-S2-03** | **连续表格边界** | 插入两个紧挨着的表格，在第二个表格首个 Cell 按 Backspace。 | 选区停留当前，不应意外合并或删除前一个表格的内容。 |

---

## 3. 文件树一致性 (FR-V3-03)

| ID | 检查场景 | 操作步骤 | 预期结果 |
| :--- | :--- | :--- | :--- |
| **QA-FT-01** | **根目录创建** | 在侧边栏非选中状态下（点击空白处），点击“新建文件”。 | 文件应直接创建在工作区根目录下。 |
| **QA-FT-02** | **目录保护** | 尝试删除一个包含文件的文件夹。 | 应弹出确认对话框提示删除。 |
| **QA-FT-03** | **状态一致性** | 在右侧编辑器修改内容（产生红点），在侧边栏重命名该文件。 | 1. 重命名成功。<br>2. 内存中的脏数据（未保存内容）应正确迁移到新文件名上，红点不丢失。 |
| **QA-FT-04** | **搜索定位** | 通过侧边栏搜索框查找文件并点击。 | 编辑器应正确切换到该文件，且侧边栏保持该文件的选中同步。 |

---

## 4. Tauri 环境专项（仅桌面版验证）

| ID | 检查场景 | 操作步骤 | 预期结果 |
| :--- | :--- | :--- | :--- |
| **QA-TR-01** | **原生保存触发** | 修改内容后直接按 `Ctrl+S`。 | 预览控制台逻辑显示“Save triggered via Cmd+S”，触发自动保存。 |
| **QA-TR-02** | **关窗保存流** | 修改内容后（显示红点），直接点击窗口右上角 [X] 关闭。 | 1. 窗口延时关闭（执行 Flush）。<br>2. 重新打开应用，刚才修改的内容已持久化到磁盘。 |

---

## 5. 验收门禁自检 (DoD)

- [ ] **自动化测试**：`npm run test` 在本地运行全部 Pass (89 cases)。
- [ ] **构建校验**：执行 `npm run build` 无 Error/Warning。
- [ ] **Lint 校验**：无残留的 `prettier` 或 `eslint` 报错。
- [ ] **性能基准**：加载 500kb 以上 Markdown 文件无感知卡顿。
