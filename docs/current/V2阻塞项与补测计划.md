# V2 阻塞项与补测计划

## 1. 阻塞项说明

- **现象描述**：在 `http://127.0.0.1:4173/` 的浏览器 Web 开发模式下，无法触发原生文件夹选择流程。
- **根本原因**：localhost web-mode 环境限制，无法调用 Tauri 原生文件系统 API。
- **阻塞影响**：导致无法完整执行侧边栏搜索与编辑器查找替换的端到端（E2E）手工验收。

## 2. 受影响范围

- **功能模块**：侧边栏搜索 (Sidebar Search) + 查找/替换 (Find/Replace)。
- **验证环节**：端到端手工 QA 流程。

## 3. 阶段性替代验证（Interim Verification）

在进入 Tauri 原生环境补测前，已通过以下自动化手段确保逻辑正确性：

- [x] **单元测试**：`npm run test` 通过（80/80）。
- [x] **构建验证**：`npm run build` 成功，无编译错误。

## 4. 补测计划与通过标准

待切换至 Tauri-native 环境后，需执行以下补测动作：

### 补测动作

1. 在 Tauri 原生壳中启动应用。
2. 触发文件夹选择器并成功加载本地目录。
3. 执行侧边栏全局搜索，验证结果匹配度。
4. 执行编辑器内查找与替换，验证文件内容变更。

### 补测通过标准

- [ ] 原生文件夹选择器可正常唤起并返回路径。
- [ ] 侧边栏搜索结果可实时更新且点击可定位。
- [ ] 查找替换逻辑在原生文件系统下读写正常。
- [ ] 全流程无崩溃或权限报错。

---

_文档状态：待补测 (Pending Native Test)_
