# 决策台账（冻结资产）

## D-001 平台范围

- 决策：V1 必做 macOS、Windows；Linux 可选。
- 状态：已冻结

## D-002 非目标（Not in V1）

- 决策：插件生态、双链/图谱、云同步/协作/账号、分屏预览、Vim/Emacs、流程图、表格公式、智能引号、文件标签属性均不进入 V1。
- 状态：已冻结

## D-003 工作区模式

- 决策：V1 仅支持单工作区（单根目录）。
- 状态：已冻结

## D-004 图片命名规则

- 决策：`YYYYMMDD-HHMMSS-随机后缀.png`，保存到文档同级 `assets/`，相对路径引用。
- 状态：已冻结

## D-005 自动保存策略

- 决策：800ms 防抖 + 失焦保存 + 关闭前强制保存。
- 状态：已冻结

## D-006 表格能力优先级

- 决策：不进 V1；列为 Phase 2 Top Priority。
- 状态：已冻结

## D-007 TipTap Roundtrip 门禁

- 决策：Step D 结束前必须完成 Roundtrip Spike，未通过不得进入 Step E。
- 状态：已冻结

## D-008 Tauri 回退触发

- 决策：仅在 P0 阻塞 + 可复现 + 两轮修复失败 + 评审确认时，才评估回退 Electron。
- 状态：已冻结

## D-009 文件原子写入

- 决策：保存必须采用“临时文件写入 -> rename 覆盖”。
- 状态：已冻结

## D-010 编辑器回退策略

- 决策：不预设强制切 CodeMirror 6；若 Spike 失败，进入备选内核评估并评审决策。
- 状态：已冻结
