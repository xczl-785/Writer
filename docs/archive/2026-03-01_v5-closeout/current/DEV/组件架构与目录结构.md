# V5 组件架构与目录结构

**版本**: V5.0  
**创建日期**: 2026-02-28  
**归属角色**: 前端架构师 (Oracle)  
**状态**: 设计完成

---

## 一、目录结构总览

```
src/
├── app/                           # 应用入口 (现有)
│   ├── App.tsx
│   └── closeWorkflow.ts
│
├── ui/                            # UI 组件层
│   ├── components/                # V5 新增通用组件
│   │   ├── ContextMenu/           # 右键菜单
│   │   │   ├── ContextMenu.tsx
│   │   │   ├── MenuItem.tsx
│   │   │   ├── MenuDivider.tsx
│   │   │   ├── contextMenuRegistry.ts
│   │   │   └── index.ts
│   │   │
│   │   ├── BubbleMenu/            # 选区浮动菜单
│   │   │   ├── BubbleMenu.tsx
│   │   │   ├── BubbleMenuItem.tsx
│   │   │   └── index.ts
│   │   │
│   │   ├── Dialog/                # 对话框
│   │   │   ├── Dialog.tsx
│   │   │   ├── ConfirmDialog.tsx
│   │   │   ├── DialogOverlay.tsx
│   │   │   ├── useDialog.ts
│   │   │   └── index.ts
│   │   │
│   │   ├── Tooltip/               # 悬浮提示
│   │   │   ├── Tooltip.tsx
│   │   │   ├── useTooltip.ts
│   │   │   └── index.ts
│   │   │
│   │   └── StatusIndicator/       # 状态指示器
│   │       ├── StatusIndicator.tsx
│   │       ├── StatusDot.tsx
│   │       ├── ErrorPanel.tsx
│   │       ├── statusMachine.ts
│   │       └── index.ts
│   │
│   │   ├── BlockBoundary/         # 块级元素边界提示 (US-B3)
│   │   │   ├── BlockBoundary.tsx
│   │   │   ├── GutterLine.tsx
│   │   │   ├── useBlockBoundary.ts
│   │   │   └── index.ts
│   │   │
│   │   └── Breadcrumb/            # 面包屑导航 (US-A2)
│   │       ├── Breadcrumb.tsx
│   │       ├── BreadcrumbItem.tsx
│   │       ├── useBreadcrumb.ts
│   │       └── index.ts
│   ├── editor/                    # 编辑器组件 (现有 + 扩展)
│   │   ├── Editor.tsx
│   │   ├── extensions/
│   │   │   ├── contextMenuExtension.ts    # 编辑器右键菜单扩展
│   │   │   ├── bubbleMenuExtension.ts     # Bubble Menu 扩展
│   │   │   └── index.ts
│   │   ├── components/
│   │   │   ├── Toolbar.tsx
│   │   │   ├── EditorShell.tsx
│   │   │   ├── BreadcrumbBar.tsx   # 面包屑导航栏
│   │   │   └── ...
│   │
│   ├── sidebar/                   # 侧边栏组件 (现有 + 扩展)
│   │   ├── Sidebar.tsx
│   │   ├── Outline/               # V5 新增大纲面板
│   │   │   ├── Outline.tsx
│   │   │   ├── OutlineItem.tsx
│   │   │   ├── OutlineVirtualized.tsx    # 虚拟滚动版本
│   │   │   ├── useOutlineExtractor.ts
│   │   │   └── index.ts
│   │   └── ...
│   │
│   └── statusbar/                 # 状态栏组件 (现有)
│       ├── StatusBar.tsx
│       └── ...
│
├── state/                         # 状态管理 (现有 + 扩展)
│   ├── slices/
│   │   ├── workspaceSlice.ts
│   │   ├── editorSlice.ts
│   │   ├── filetreeSlice.ts
│   │   ├── statusSlice.ts         # V5 扩展: 保存状态
│   │   └── uiSlice.ts             # V5 新增: UI 状态
│   └── ...
│
├── services/                      # 服务层 (现有)
│   └── ...
│
├── hooks/                         # V5 新增: 通用 Hooks
│   ├── useContextMenu.ts
│   ├── useKeyboardShortcut.ts
│   ├── usePlatform.ts             # 平台检测 (Mac/Win/Linux)
│   └── index.ts
│
└── utils/                         # 工具函数 (现有 + 扩展)
    ├── keyboard.ts                # 快捷键工具
    ├── platform.ts                # 平台相关
    └── ...
```

---

## 二、组件详细设计

### 2.1 ContextMenu 组件

**职责**: 渲染右键菜单，支持动态菜单项注册

**Props**:

```typescript
interface ContextMenuProps {
  x: number; // 显示位置 X
  y: number; // 显示位置 Y
  context: string; // 上下文标识 (如 'filetree', 'editor')
  onClose: () => void;
}
```

**依赖**: `contextMenuRegistry.ts`

**样式规范**: `prototypes/01_context_menus.html`

- 背景: `bg-white dark:bg-zinc-800`
- 边框: `border border-zinc-200 dark:border-zinc-700`
- 阴影: `shadow-xl`
- 圆角: `rounded-md`
- 菜单项高度: `h-8` 或 `py-1.5 px-3`

### 2.2 BubbleMenu 组件

**职责**: 选中文本时显示格式化工具栏

**集成方式**: TipTap BubbleMenu 扩展

**样式规范**: `prototypes/01_context_menus.html`

- 背景: `bg-zinc-800 text-white`
- 边框: `border border-zinc-700`
- 阴影: `shadow-lg`
- 圆角: `rounded-md`

### 2.3 StatusIndicator 组件

**职责**: 显示文档保存状态 (四态流转)

**Props**:

```typescript
interface StatusIndicatorProps {
  status: 'saved' | 'dirty' | 'saving' | 'error';
  message?: string;
  errorDetails?: ErrorDetails;
}
```

**状态流转**:

```
saved ──(edit)──► dirty ──(save)──► saving ──(success)──► saved
  ▲                                              │
  │                                           (error)
  │                                              │
  └──────────────────────────────────────────────┘
                      │
                      ▼
                    error
```

**样式规范**: `prototypes/02_status_indicator.html`

- 已保存: `bg-emerald-500` 绿色圆点
- 未保存: `bg-amber-400` 黄色实心
- 保存中: `bg-blue-500 animate-pulse` 蓝色呼吸灯
- 错误: `bg-red-500` 红色 + 悬停面板

### 2.4 Outline 组件

**职责**: 显示文档大纲，支持点击跳转

**Props**:

```typescript
interface OutlineProps {
  editor: Editor;
  onItemClick?: (item: OutlineItem) => void;
}
```

**性能策略**:

- < 100 项: 全量渲染
- 100-500 项: 防抖更新 (150ms)
- > 500 项: 虚拟滚动

**样式规范**: `prototypes/03_outline_dialogs.html`

- 层级缩进: `pl-3` / `pl-6` / `pl-9`
- 激活项: `text-blue-600 bg-blue-50/50 border-blue-500`
- 悬停: `hover:text-blue-600 hover:bg-blue-50`

### 2.5 Dialog 组件

**职责**: 通用对话框容器 (用于非 Tauri 原生场景)

**Props**:

```typescript
interface DialogProps {
  open: boolean;
  onClose: () => void;
  title: string;
  children: React.ReactNode;
  actions?: React.ReactNode;
  size?: 'sm' | 'md' | 'lg';
}
```

**样式规范**: `prototypes/03_outline_dialogs.html`

- 遮罩: `bg-black/20`
- 面板: `bg-white border border-zinc-200 shadow-2xl rounded-lg`
- 宽度: `max-w-md`

### 2.6 BlockBoundary 组件 (US-B3)

**职责**: 为代码块、引用块等块级元素提供视觉边界提示

**Props**:

```typescript
interface BlockBoundaryProps {
  type: 'codeBlock' | 'blockquote' | 'listBlock' | 'table';
  position: number; // 文档位置
  active: boolean;   // 光标是否在块内
}

interface GutterLineProps {
  type: 'codeBlock' | 'blockquote' | 'listBlock' | 'table';
  visible: boolean;
}
```

**实现方式**: TipTap Decoration + ProseMirror Plugin

```typescript
// src/ui/editor/extensions/blockBoundaryExtension.ts
import { Extension } from '@tiptap/core';
import { Plugin, PluginKey } from 'prosemirror-state';
import { Decoration, DecorationSet } from 'prosemirror-view';

export const BlockBoundaryExtension = Extension.create({
  name: 'blockBoundary',

  addProseMirrorPlugins() {
    return [
      new Plugin({
        key: new PluginKey('blockBoundary'),
        props: {
          decorations: (state) => {
            // 监听选区变化，判断光标所在块类型
            // 返回对应的 Decoration
          },
        },
      }),
    ];
  },
});
```

**样式规范**: `UI_UX规范.md` 第五节

- 代码块装订线: `w-1 bg-blue-400/50 rounded-l-sm`
- 引用块装订线: `border-l-2 border-zinc-400`
- 显示动画: `duration-50 ease-out`
- 隐藏动画: `duration-100 ease-in`

### 2.7 Breadcrumb 组件 (US-A2)

**职责**: 显示当前文件的路径面包屑，支持点击跳转目录

**Props**:

```typescript
interface BreadcrumbProps {
  path: BreadcrumbItem[];
  maxVisible?: number; // 默认 4
  onItemClick?: (item: BreadcrumbItem, index: number) => void;
}

interface BreadcrumbItem {
  id: string;
  name: string;
  type: 'workspace' | 'folder' | 'file';
  path: string;
}
```

**位置**: 编辑器顶部，紧贴工具栏下方

**样式规范**: `UI_UX规范.md` 第六节

- 容器: `h-7 bg-zinc-50 dark:bg-zinc-900 px-4 py-1`
- 文本: `text-sm text-zinc-600 dark:text-zinc-400`
- 分隔符: `text-zinc-300 dark:text-zinc-600 mx-1.5` (使用 `›` 符号)
- 当前项: `text-zinc-800 dark:text-zinc-200 font-medium`
- 悬停: `hover:text-blue-500 hover:underline`

**截断策略**:

- 路径超过 4 级时中部截断
- 保留首项和末两项
- 悬停截断区域显示完整路径 Tooltip

**Hook**: `useBreadcrumb.ts`

```typescript
// src/ui/components/Breadcrumb/useBreadcrumb.ts
interface UseBreadcrumbReturn {
  items: BreadcrumbItem[];
  truncatedItems: BreadcrumbItem[];
  isTruncated: boolean;
  handleItemClick: (item: BreadcrumbItem) => void;
}

// 从 workspaceSlice 获取当前文件路径
// 解析路径生成面包屑项数组
// 处理截断逻辑
```
### 2.6 BlockBoundary 组件 (US-B3)

**职责**: 为代码块、引用块等块级元素提供视觉边界提示

**Props**:

```typescript
interface BlockBoundaryProps {
  type: 'codeBlock' | 'blockquote' | 'listBlock' | 'table';
  position: number; // 文档位置
  active: boolean;   // 光标是否在块内
}

interface GutterLineProps {
  type: 'codeBlock' | 'blockquote' | 'listBlock' | 'table';
  visible: boolean;
}
```

**实现方式**: TipTap Decoration + ProseMirror Plugin

```typescript
// src/ui/editor/extensions/blockBoundaryExtension.ts
import { Extension } from '@tiptap/core';
import { Plugin, PluginKey } from 'prosemirror-state';
import { Decoration, DecorationSet } from 'prosemirror-view';

export const BlockBoundaryExtension = Extension.create({
  name: 'blockBoundary',

  addProseMirrorPlugins() {
    return [
      new Plugin({
        key: new PluginKey('blockBoundary'),
        props: {
          decorations: (state) => {
            // 监听选区变化，判断光标所在块类型
            // 返回对应的 Decoration
          },
        },
      }),
    ];
  },
});
```

**样式规范**: `UI_UX规范.md` 第五节

- 代码块装订线: `w-1 bg-blue-400/50 rounded-l-sm`
- 引用块装订线: `border-l-2 border-zinc-400`
- 显示动画: `duration-50 ease-out`
- 隐藏动画: `duration-100 ease-in`

### 2.7 Breadcrumb 组件 (US-A2)

**职责**: 显示当前文件的路径面包屑，支持点击跳转目录

**Props**:

```typescript
interface BreadcrumbProps {
  path: BreadcrumbItem[];
  maxVisible?: number; // 默认 4
  onItemClick?: (item: BreadcrumbItem, index: number) => void;
}

interface BreadcrumbItem {
  id: string;
  name: string;
  type: 'workspace' | 'folder' | 'file';
  path: string;
}
```

**位置**: 编辑器顶部，紧贴工具栏下方

**样式规范**: `UI_UX规范.md` 第六节

- 容器: `h-7 bg-zinc-50 dark:bg-zinc-900 px-4 py-1`
- 文本: `text-sm text-zinc-600 dark:text-zinc-400`
- 分隔符: `text-zinc-300 dark:text-zinc-600 mx-1.5` (使用 `›` 符号)
- 当前项: `text-zinc-800 dark:text-zinc-200 font-medium`
- 悬停: `hover:text-blue-500 hover:underline`

**截断策略**:

- 路径超过 4 级时中部截断
- 保留首项和末两项
- 悬停截断区域显示完整路径 Tooltip

**Hook**: `useBreadcrumb.ts`

```typescript
// src/ui/components/Breadcrumb/useBreadcrumb.ts
interface UseBreadcrumbReturn {
  items: BreadcrumbItem[];
  truncatedItems: BreadcrumbItem[];
  isTruncated: boolean;
  handleItemClick: (item: BreadcrumbItem) => void;
}

// 从 workspaceSlice 获取当前文件路径
// 解析路径生成面包屑项数组
// 处理截断逻辑
---

## 三、状态管理设计

### 3.1 uiSlice (V5 新增)

```typescript
interface UIState {
  contextMenu: {
    open: boolean;
    x: number;
    y: number;
    context: string | null;
  };
  dialog: {
    open: boolean;
    type: 'delete-confirm' | 'error-detail' | null;
    data: Record<string, unknown>;
  };
  outline: {
    open: boolean;
    items: OutlineItem[];
    activeId: string | null;
  };
}
```

### 3.2 statusSlice (V5 扩展)

```typescript
interface StatusState {
  saveStatus: 'saved' | 'dirty' | 'saving' | 'error';
  lastSavedAt: number | null;
  errorMessage: string | null;
  errorDetails: ErrorDetails | null;
}
```

---

## 四、事件流设计

```
┌─────────────────────────────────────────────────────────────────┐
│                         用户交互                                 │
├─────────────────────────────────────────────────────────────────┤
│  右键点击 ──► contextmenu event ──► useContextMenu.show()       │
│  文本选择 ──► selectionchange ──► BubbleMenu 自动显示            │
│  大纲点击 ──► onItemClick ──► editor.commands.setTextSelection  │
│  保存触发 ──► autosave.schedule ──► statusSlice.transition      │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                         状态更新                                 │
├─────────────────────────────────────────────────────────────────┤
│  uiSlice ──► contextMenu.open = true                            │
│  statusSlice ──► saveStatus = 'saving'                          │
│  editorSlice ──► outlineItems = extractedItems                  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                         视图渲染                                 │
├─────────────────────────────────────────────────────────────────┤
│  ContextMenu 组件渲染 ──► Portal 挂载到 body                     │
│  StatusIndicator 组件渲染 ──► 状态栏插槽                         │
│  Outline 组件渲染 ──► 侧边栏区域                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 五、组件依赖关系

```
                    ┌──────────────┐
                    │   ContextMenu   │
                    │   Registry    │
                    └──────┬───────┘
                           │
          ┌────────────────┼────────────────┐
          ▼                ▼                ▼
    ┌──────────┐    ┌──────────┐    ┌──────────┐
    │ FileTree │    │  Editor  │    │  Table   │
    │ContextMenu│   │ContextMenu│   │ContextMenu│
    └──────────┘    └──────────┘    └──────────┘
                           │
                           ▼
                    ┌──────────┐
                    │BubbleMenu│
                    └──────────┘
                           │
                           ▼
                    ┌──────────┐
                    │ StatusIndicator │
                    └────┬─────┘
                         │
                         ▼
                    ┌──────────┐
                    │ Outline  │
                    └────┬─────┘
                         │
          ┌──────────────┴──────────────┐
          ▼                             ▼
    ┌──────────┐                 ┌──────────┐
    │BlockBoundary│               │ Breadcrumb│
    │  (US-B3)  │                 │  (US-A2)  │
    └──────────┘                 └──────────┘

---

## 六、文件命名规范

| 类型     | 命名规范             | 示例                       |
| -------- | -------------------- | -------------------------- |
| 组件     | PascalCase           | `ContextMenu.tsx`          |
| Hook     | camelCase + use 前缀 | `useContextMenu.ts`        |
| 工具函数 | camelCase            | `contextMenuRegistry.ts`   |
| 类型定义 | PascalCase           | `ContextMenuTypes.ts`      |
| 样式     | 同组件名             | `ContextMenu.css` (如需要) |
| 测试     | 同组件名 + .test     | `ContextMenu.test.ts`      |

---

## 七、组件实现优先级

| 优先级 | 组件                   | 依赖                | 预计工作量 |
| ------ | ---------------------- | ------------------- | ---------- |
| P0-1   | StatusIndicator        | 无                  | 2h         |
| P0-2   | contextMenuRegistry    | 无                  | 2h         |
| P0-3   | ContextMenu            | contextMenuRegistry | 4h         |
| P0-4   | MenuItem / MenuDivider | ContextMenu         | 2h         |
| P0-5   | contextMenuExtension   | ContextMenu         | 3h         |
| P0-6   | Outline                | 无                  | 4h         |
| **P0-7**   | **BlockBoundary (US-B3)**  | TipTap Decoration   | **3h**     |
| P1-1   | BubbleMenu             | TipTap 扩展         | 3h         |
| P1-2   | Dialog                 | 无                  | 2h         |
| P1-3   | Tooltip                | 无                  | 2h         |
| **P1-4**   | **Breadcrumb (US-A2)**     | workspaceSlice      | **2h**     |
---

_本文档由 Oracle (前端架构师) 维护_
