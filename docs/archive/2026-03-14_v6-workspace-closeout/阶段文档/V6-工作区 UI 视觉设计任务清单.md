# V6 工作区 UI 视觉设计任务清单

**文档类型**: UI 设计任务清单 (UI Task List)  
**版本**: V6  
**创建日期**: 2026-03-08  
**状态**: 待设计  
**设计负责人**: 待定  
**UI 设计师**: 待定

---

## 1. 任务概述

### 1.1 设计背景

V6 工作区功能需要新增 6 个 UI 组件的视觉设计。本文档列出所有需要设计的组件、设计要求和交付物清单。

### 1.2 设计原则

| 原则   | 含义                 | 设计指导                           |
| ------ | -------------------- | ---------------------------------- |
| 沉静   | 不炫技、不打扰       | 使用低饱和度颜色，避免高对比度反馈 |
| 专注   | 让内容成为焦点       | UI 元素简洁，减少视觉噪音          |
| 可信赖 | 行为可预测           | 与 macOS/Windows 平台惯例一致      |
| 无打断 | Non-Disruption First | 优先使用非模态通知                 |

### 1.3 设计工具

**推荐工具**: Figma（首选）、Sketch、Adobe XD

**设计系统**: 如项目已有设计系统，请对齐现有令牌；如无，使用本清单定义的临时令牌。

---

## 2. 设计任务清单

### 2.1 EmptyStateWorkspace（空状态界面）

**优先级**: P0  
**设计复杂度**: ⭐⭐⭐⭐  
**使用场景**: 用户启动 Writer 但未打开任何工作区时显示

#### 设计内容

| 元素                 | 设计要求                             | 备注                         |
| -------------------- | ------------------------------------ | ---------------------------- |
| Logo/字标            | 居中，距顶部 80px                    | 使用项目现有 Logo            |
| 标语                 | "写作，心流"或"打开文件夹，开始写作" | 字号小于 Logo，颜色锌灰 -400 |
| 主按钮（打开文件夹） | 主按钮样式，最小宽度 140px           | 高优先级操作                 |
| 次按钮（打开工作区） | 次按钮样式，最小宽度 140px           | 可弱化视觉权重               |
| 分隔线               | 1px solid zinc-200，上下留白 24px    | -                            |
| 最近项目列表         | 最多 10 项，垂直排列，间距 8px       | 分组：工作区/文件夹/文件     |
| 拖拽提示             | 斜体，zinc-400 色                    | "或拖拽文件夹到此处"         |

#### 交互状态

| 状态       | 设计说明                                 |
| ---------- | ---------------------------------------- |
| 默认       | 上述布局                                 |
| 拖拽悬停   | 边框 1px solid zinc-300 + 背景 zinc-50   |
| 拖拽无效   | 红色边框 + 禁止图标 + 提示"仅支持文件夹" |
| 空历史记录 | 隐藏最近项目区域，仅显示主按钮           |

#### 响应式设计

| 断点         | 设计调整                              |
| ------------ | ------------------------------------- |
| 宽度 < 600px | 按钮垂直排列                          |
| 宽度 < 400px | 隐藏标语，仅保留 Logo 和按钮          |
| 高度 < 500px | 减少顶部留白，最近项目列表限制为 5 项 |

#### 交付物

- [ ] Figma 设计稿（默认状态）
- [ ] 拖拽悬停状态设计稿
- [ ] 响应式布局说明（<600px、<400px）
- [ ] 深色模式设计稿（可选，V6.1）

---

### 2.2 MultiRootFileTree（多根文件树容器）

**优先级**: P0  
**设计复杂度**: ⭐⭐⭐  
**使用场景**: 侧边栏文件树，支持多个根文件夹同时显示

#### 设计内容

| 元素           | 设计要求                                       | 备注                |
| -------------- | ---------------------------------------------- | ------------------- |
| 根文件夹头部   | 左对齐文件夹图标 + 显示名，右对齐展开/折叠按钮 | 悬停时背景 zinc-100 |
| 根文件夹分隔线 | 1px solid zinc-200                             | 分隔不同根文件夹    |
| 缩进层级       | 根文件夹 0px，一级子项 16px，每层+16px         | -                   |
| 展开/折叠按钮  | **16x16**，悬停显示                            | 使用三角图标或 +/-  |

#### 交互状态

| 状态         | 设计说明                 |
| ------------ | ------------------------ |
| 根文件夹悬停 | 背景 zinc-100            |
| 展开状态     | 三角图标向下 ▼           |
| 折叠状态     | 三角图标向右 ▶           |
| 选中文件     | 背景 blue-50（浅色模式） |

#### 交付物

- [ ] Figma 设计稿（多根文件树示例）
- [ ] 展开/折叠状态设计稿
- [ ] 缩进规范说明

---

### 2.3 WorkspaceRootHeader（根文件夹头部组件）

**优先级**: P0  
**设计复杂度**: ⭐⭐  
**使用场景**: 多根文件树中每个根文件夹的头部区域

#### 设计内容

| 元素           | 设计要求                   | 备注                 |
| -------------- | -------------------------- | -------------------- |
| 文件夹图标     | 16x16，左对齐              | 使用项目现有图标系统 |
| 显示名         | 截断显示，最大长度 20 字符 | 超长显示省略号       |
| 展开/折叠按钮  | 12x12，右侧                | 悬停显示             |
| 右键菜单触发区 | 整个头部区域               | 右键点击触发菜单     |

#### 交互状态

| 状态     | 设计说明                                 |
| -------- | ---------------------------------------- |
| 默认     | 正常显示                                 |
| 悬停     | 背景 zinc-100                            |
| 右键菜单 | 显示"重命名显示名"、"从工作区移除"等选项 |

#### 交付物

- [ ] Figma 设计稿（默认 + 悬停状态）
- [ ] 右键菜单设计稿

---

### 2.4 WorkspaceIndicator（状态栏工作区指示器）

**优先级**: P1  
**设计复杂度**: ⭐  
**使用场景**: 状态栏右侧显示当前工作区名称

#### 设计内容

| 元素       | 设计要求                      | 备注                   |
| ---------- | ----------------------------- | ---------------------- |
| 工作区名称 | 次要色 zinc-500               | 与文件编码信息区分层级 |
| 最大宽度   | 150px                         | 超长截断显示省略号     |
| 分隔符     | 竖线 `│` 与状态栏其他信息分隔 | -                      |

#### 状态变体

| 状态           | 显示内容              | 颜色            |
| -------------- | --------------------- | --------------- |
| 单文件夹       | 文件夹名称            | zinc-500        |
| 多文件夹已保存 | 工作区名称 (工作区)   | zinc-500        |
| 无标题工作区   | 未命名工作区 (未保存) | zinc-400 + 斜体 |

#### 交付物

- [ ] Figma 设计稿（三种状态）
- [ ] 状态栏整体布局说明

---

### 2.5 RecentItemsMenu（最近项目菜单）

**优先级**: P1  
**设计复杂度**: ⭐⭐  
**使用场景**: `文件 > 打开最近` 子菜单

#### 设计内容

| 元素     | 设计要求                       | 备注                    |
| -------- | ------------------------------ | ----------------------- |
| 分组标题 | "工作区"、"文件夹"、"文件"     | 次要色 zinc-400，小字号 |
| 工作区项 | 💼 图标 + 名称 + (工作区) 后缀 | 最多 10 条              |
| 文件夹项 | 📁 图标 + 路径                 | 最多 5 条               |
| 文件项   | 📄 图标 + 路径                 | 最多 20 条              |
| 悬停状态 | 背景 zinc-100                  | -                       |
| 右键菜单 | "从最近移除"选项               | -                       |

#### 图标规范

| 类型   | 图标                  | 颜色     |
| ------ | --------------------- | -------- |
| 工作区 | 💼 或自定义公文包图标 | zinc-600 |
| 文件夹 | 📁 或自定义文件夹图标 | zinc-600 |
| 文件   | 📄 或自定义文档图标   | zinc-600 |

**注意**: 如使用 emoji，需确保跨平台一致性；否则使用项目图标系统。

#### 交付物

- [ ] Figma 设计稿（菜单展开状态）
- [ ] 图标规范说明

---

### 2.6 FileConflictDialog（文件冲突对话框）

**优先级**: P1  
**设计复杂度**: ⭐⭐⭐  
**使用场景**: 外部文件被修改时提示用户选择操作

#### 设计内容

| 元素         | 设计要求                       | 备注               |
| ------------ | ------------------------------ | ------------------ |
| 对话框标题   | "文件已被外部修改"             | -                  |
| 文件信息     | 文件名 + 路径 + 修改时间对比   | 使用表格或列表布局 |
| 操作按钮     | "比较变更"、"重新加载"、"忽略" | 主按钮为"重新加载" |
| 不再询问选项 | 复选框"不再询问此类文件"       | 底部对齐           |

#### 布局示例

```
┌─────────────────────────────────────────┐
│                                         │
│  📄 document.md 已被外部修改             │
│                                         │
│  您的版本：2026-03-08 10:30 AM          │
│  外部版本：2026-03-08 11:45 AM          │
│                                         │
│     [比较变更]  [重新加载]  [忽略]       │
│                                         │
│  □ 不再询问此类文件                     │
│                                         │
└─────────────────────────────────────────┘
```

#### 交付物

- [ ] Figma 设计稿（对话框）
- [ ] 响应式布局说明（小屏幕适配）

---

### 2.7 DragFeedbackStates（拖拽反馈状态组）

**优先级**: P0  
**设计复杂度**: ⭐⭐⭐  
**使用场景**: 所有拖拽操作的视觉反馈

#### 设计内容

| 状态             | 设计要求                                     | 备注                   |
| ---------------- | -------------------------------------------- | ---------------------- |
| 空状态拖拽悬停   | 边框 1px solid zinc-300 + 背景 zinc-50       | 整个区域高亮           |
| 空状态拖拽无效   | 红色边框 + 禁止图标 + 提示                   | 仅支持文件夹           |
| 文件树拖拽悬停   | 边框 1px solid zinc-300                      | 文件树区域             |
| 插入位置指示器   | 项间蓝色横线 2px (blue-600)                  | 内部文件移动           |
| 复制拖拽光标     | 光标显示 `+` 号                              | Cmd/Ctrl + 拖拽时      |
| 无效文件类型提示 | 红色边框 + 禁止图标 + 提示"不支持的文件类型" | 非 Markdown 文件拖入时 |

#### 交付物

- [ ] Figma 设计稿（6 种拖拽状态）
- [ ] 光标变化规范

---

### 2.8 SaveWorkspacePrompt（保存工作区提示）

**优先级**: P1  
**设计复杂度**: ⭐⭐  
**使用场景**: 关闭无标题工作区时状态栏非模态通知

#### 设计内容

| 元素     | 设计要求                    | 备注            |
| -------- | --------------------------- | --------------- |
| 通知背景 | 状态栏背景色                | 与状态栏一致    |
| 图标     | 💡 提示图标                 | 16x16           |
| 文案     | "多文件夹工作区可保存为..." | 次要色 zinc-500 |
| 操作按钮 | [保存] [不保存] [不再提醒]  | 紧凑布局        |
| 自动消失 | 5 秒后自动消失              | 可配置          |

#### 交付物

- [ ] Figma 设计稿（状态栏通知）
- [ ] 动画规范（出现/消失）

---

### 2.9 ErrorStates（错误状态组）

**优先级**: P2  
**设计复杂度**: ⭐⭐  
**使用场景**: 边缘情况错误状态显示

#### 设计内容

| 错误类型       | 设计要求                        | 备注         |
| -------------- | ------------------------------- | ------------ |
| 文件夹不存在   | 灰显 + ⚠️ 图标 + "(不存在)"     | 文件树中     |
| 工作区文件损坏 | 状态栏错误通知 + [创建新工作区] | 红色边框     |
| 权限不足       | 错误提示 + [在访达中显示]       | 打开文件夹时 |

#### 交付物

- [ ] Figma 设计稿（3 种错误状态）
- [ ] 图标规范

---

## 3. 设计令牌（临时）

如项目无现有设计系统，请使用以下临时令牌：

### 3.1 颜色

| 令牌                     | 值（浅色模式）          | 值（深色模式）     | 用途       |
| ------------------------ | ----------------------- | ------------------ | ---------- |
| `--color-surface`        | #ffffff                 | #1a1a1a            | 背景色     |
| `--color-surface-hover`  | #f4f4f5 (zinc-100)      | #2a2a2a            | 悬停背景   |
| `--color-surface-active` | #e4e4e7 (zinc-200)      | #3a3a3a            | 激活背景   |
| `--color-border`         | #e4e4e7 (zinc-200)      | #3f3f3f            | 边框       |
| `--color-text-primary`   | #18181b (zinc-900)      | #f4f4f5            | 主文字     |
| `--color-text-secondary` | #71717a (zinc-500)      | #a1a1aa            | 次要文字   |
| `--color-text-tertiary`  | #a1a1aa (zinc-400)      | #71717a            | 第三级文字 |
| `--color-accent`         | **#2563eb (blue-600)**  | #60a5fa (blue-400) | 强调色     |
| `--color-accent-subtle`  | #dbeafe (blue-100)      | #1e3a8a (blue-900) | 强调色背景 |
| `--color-error`          | #ef4444 (red-500)       | #f87171            | 错误色     |
| `--color-success`        | **#22c55e (green-500)** | #4ade80            | 成功色     |
| `--color-focus-ring`     | **#2563eb (blue-600)**  | #60a5fa            | 焦点环     |

### 3.2 间距

| 令牌           | 值   | 用途       |
| -------------- | ---- | ---------- |
| `--spacing-xs` | 4px  | 最小间距   |
| `--spacing-sm` | 8px  | 小组件间距 |
| `--spacing-md` | 16px | 标准间距   |
| `--spacing-lg` | 24px | 大间距     |
| `--spacing-xl` | 32px | 超大间距   |

### 3.3 字体

| 令牌             | 值   | 用途     |
| ---------------- | ---- | -------- |
| `--font-size-xs` | 11px | 极小文字 |
| `--font-size-sm` | 12px | 小文字   |
| `--font-size-md` | 14px | 标准文字 |
| `--font-size-lg` | 16px | 大文字   |
| `--font-size-xl` | 20px | 标题     |

### 3.4 圆角

| 令牌          | 值  | 用途     |
| ------------- | --- | -------- |
| `--radius-sm` | 4px | 小圆角   |
| `--radius-md` | 6px | 标准圆角 |
| `--radius-lg` | 8px | 大圆角   |

### 3.5 焦点指示器

| 令牌                 | 值                 | 用途           |
| -------------------- | ------------------ | -------------- |
| `--color-focus-ring` | #2563eb (blue-600) | 键盘焦点环颜色 |
| `--outline-width`    | 2px                | 焦点环宽度     |
| `--outline-offset`   | 2px                | 焦点环偏移     |

### 3.6 动画令牌

| 令牌                  | 值    | 用途     |
| --------------------- | ----- | -------- |
| `--transition-fast`   | 100ms | 快速过渡 |
| `--transition-normal` | 150ms | 标准过渡 |
| `--transition-slow`   | 200ms | 慢速过渡 |

---

## 4. 交付要求

### 4.1 设计稿格式

- **工具**: Figma（首选）、Sketch、Adobe XD
- **文件命名**: `V6-Workspace-{组件名}.fig`
- **画板命名**: `{组件名}-{状态}`（如 `EmptyState-Default`、`EmptyState-DragHover`）

### 4.2 设计说明

每个组件需附带：

1. **使用场景说明**: 何时显示该组件
2. **交互状态**: 默认、悬停、激活、禁用的视觉差异
3. **响应式规则**: 不同屏幕尺寸下的布局调整
4. **深色模式**（可选）: V6.1 前完成

### 4.3 交付时间

| 组件                    | 优先级 | 建议完成时间 |
| ----------------------- | ------ | ------------ |
| EmptyStateWorkspace     | P0     | 1 天         |
| MultiRootFileTree       | P0     | 1 天         |
| WorkspaceRootHeader     | P0     | 0.5 天       |
| WorkspaceIndicator      | P1     | 0.5 天       |
| RecentItemsMenu         | P1     | 0.5 天       |
| FileConflictDialog      | P1     | 1 天         |
| **DragFeedbackStates**  | **P0** | **1 天**     |
| **SaveWorkspacePrompt** | **P1** | **0.5 天**   |
| **ErrorStates**         | **P2** | **0.5 天**   |

**总计**: 约 6-7 天

---

## 5. 验收标准

设计完成后，需通过以下验收：

| 验收项         | 标准                                                   |
| -------------- | ------------------------------------------------------ |
| 视觉一致性     | 与现有 Writer UI 风格一致                              |
| 平台惯例       | 符合 macOS/Windows 设计规范                            |
| **可访问性**   | **颜色对比度 ≥ 4.5:1（WCAG AA），强调色使用 blue-600** |
| 响应式         | 支持最小窗口尺寸（600x400）                            |
| 深色模式       | 所有组件都有深色模式变体（V6.1 前完成）                |
| **焦点指示器** | **键盘导航焦点环 2px blue-600**                        |

---

## 6. 全局原则对齐

| 全局原则      | V6 体现                            | 位置            |
| ------------- | ---------------------------------- | --------------- |
| **极致克制**  | 使用低饱和度颜色，避免高对比度反馈 | 1.2 节          |
| **隐形界面**  | 空状态仅在启动时显示               | 2.1 节          |
| **无打断流**  | 非模态通知优先                     | 2.8 节          |
| **中性美学**  | Zinc 色系 + blue-600 强调色        | 3.1 节          |
| **OS 原生感** | 快捷键与平台惯例一致               | 交互规范 3.1 节 |
| **呼吸感**    | 80px 顶部留白，8/16/24px 间距体系  | 2.1 节，3.2 节  |

---

## 6. 参考资料

| 资料                  | 链接/位置                                                     |
| --------------------- | ------------------------------------------------------------- |
| V6 工作区功能需求文档 | `docs/current/阶段文档/V6-工作区功能需求文档.md`              |
| V6 交互设计规范       | `docs/current/阶段文档/V6-工作区交互设计规范.md`              |
| V6 技术架构方案       | `docs/current/阶段文档/V6-工作区技术架构方案.md`              |
| 全局交互设计哲学      | `docs/全局资产/交互/交互设计哲学与通用原则.md`                |
| macOS 人机界面指南    | https://developer.apple.com/design/human-interface-guidelines |
| Windows Fluent 设计   | https://learn.microsoft.com/en-us/windows/apps/design/        |

---

**文档结束**
