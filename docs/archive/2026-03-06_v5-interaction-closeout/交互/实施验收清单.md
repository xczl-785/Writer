# Writer V5 交互实施验收清单（2026-03-04）

## 1. 自动化验证

- 命令：`npm test -- src/ui/layout/FocusZenBehavior.test.ts src/ui/editor/menus/SlashMenuBoundary.test.ts src/ui/statusbar/StatusBarVisualSpec.test.ts src/ui/components/Settings/SettingsViewModes.test.ts`
- 结果：PASS

## 2. 功能验收项（手工）

- [x] 视图菜单“打字机模式”可切换，状态重启后保持。
- [x] 进入 Zen 强制开启打字机；退出 Zen 恢复进入前偏好。
- [x] Min 档侧栏为 Overlay，点击外部可收起，Header 降级为 `... / filename`。
- [x] Editor 间距满足 `64/32 + 32/16 + 850 + 40vh`。
- [x] Focus Zen 默认隐藏 Header/StatusBar，顶/底 50px 悬浮唤醒，Esc 在无浮层时退出。
- [x] Slash Menu 支持翻转、防右溢出、低视口内滚。
- [x] 状态栏：Saving 2s pulse，Saved 30% 透明，Unsaved amber 常亮。
- [x] 设置面板编辑器页可控制 Typewriter / Focus Zen 且即时生效。
- [x] 设置弹窗 Min 模式为 Inset 20px 且无大阴影。

## 3. 已知差异与后续

- Focus Zen 独立快捷键（如 F11）尚未新增绑定。本轮保留双击侧栏按钮与 Esc 退出链路，待键位决策后补齐命令映射。
- 持久化当前为 `Zustand persist + web storage adapter`，后续需按迁移策略平滑切换到 Tauri store。

## 4. 模块 C（Focus Zen）遗留项

- Esc 退出的“活跃浮层”当前覆盖 `Slash Menu / Find Panel / Context Menu / Outline`；若后续新增编辑器浮层类型，需要同步纳入 Esc 优先级守卫。
- Focus Zen 的专用快捷键尚未固化（当前仍以双击侧栏按钮为主入口）；待键位方案确认后补齐并回归 `TC-C01~TC-C09`。
- 双击交互已改为“单击延时 + 双击优先”，需持续人工回归高频连击场景（`TC-C09`）以监控不同平台下的双击阈值体验差异。

---

## 5. 编辑区三间距收敛回归（2026-03-06）

### 5.1 自动化验证

- 命令：
  - `pnpm vitest run src/ui/editor/core/EditorLayoutModel.test.ts src/ui/editor/EditorStyles.test.ts src/ui/editor/contracts/editorContracts.test.ts src/ui/editor/components/EditorShellLayoutBehavior.test.ts src/ui/sidebar/SidebarResponsiveBehavior.test.ts src/ui/layout/useViewportTier.test.ts src/ui/editor/hooks/useTypewriterAnchor.test.ts src/ui/editor/domain/editorDomain.test.ts`
- 结果：PASS（28 tests）
- 覆盖目标：
  - 间距模型 `Top/Inline/Bottom/MaxWidth` 档位映射（含 `airy`）
  - `.ProseMirror` 变量消费契约
  - ProseMirror 不使用 `h-full` 强制高度契约
  - Min 档 Overlay 不挤压正文契约
  - Typewriter 锚点基础契约（不改算法）

### 5.2 手工回归矩阵（待验收）

| 用例ID   | 窗口档位                   | 操作步骤                  | 观察结论                     | 当前状态 |
| :----- | :--------------------- | :-------------------- | :----------------------- | :--- |
| TC-A04 | Min (`W<=640`)         | 打开任意文档，观察首屏正文边距       | Top≈32，Side≈16           | 已关闭  |
| TC-A05 | Default/Airy (`W>640`) | 拖拽窗口至 Default 与 Airy  | Top≈64，Side≈32           | 已关闭  |
| TC-A06 | Airy (`W>=1440`)       | 拉宽窗口，观察正文列宽与居中        | Max width<=850，左右留白增加且居中 | 已关闭  |
| TC-B03 | Min Overlay            | Min 档展开侧栏 Overlay     | 正文宽度不被侧栏挤压               | 已关闭  |
| TC-A08 | 任意档位                   | 滚动到文末                 | 文末仍有约 40vh 留白            | 已关闭  |
| TC-D10 | 长文文末                   | 开启 Typewriter，持续在文末输入 | 光标不贴底，底部留白持续有效           | 转第6节  |

---

## 6. Typewriter 重做与 Top Offset（Find-only）回归（2026-03-06）

### 6.1 自动化验证

- 命令：
  - `pnpm vitest run src/ui/editor/domain/typewriter/typewriterDomain.test.ts src/ui/editor/hooks/useTypewriterAnchor.test.ts src/ui/editor/hooks/useTypewriterAnchorBehavior.test.ts src/ui/editor/components/EditorShellLayoutBehavior.test.ts src/ui/editor/core/EditorLayoutModel.test.ts src/ui/editor/EditorStyles.test.ts src/ui/editor/contracts/editorContracts.test.ts src/ui/sidebar/SidebarResponsiveBehavior.test.ts src/ui/layout/useViewportTier.test.ts src/ui/editor/domain/editorDomain.test.ts`
- 结果：PASS（38 tests）
- 覆盖目标：
  - Typewriter `45% + deadband(±12px)` 纯函数行为
  - rAF 合帧与 composition 保护契约
  - 显式 `--editor-content-offset-top` 变量链路（Find-only）
  - 三间距与 Overlay 相关历史契约不回归

### 6.2 手工回归矩阵（待验收）

| 用例ID | 窗口档位 | 操作步骤 | 观察结论 | 当前状态 |
| :--- | :--- | :--- | :--- | :--- |
| TC-D01 | 长文（>2屏） | 菜单开启 Typewriter，持续输入 | 光标行稳定趋近 45% 视点 | 待执行 |
| TC-D02 | 长文 | 菜单关闭 Typewriter | 停止锚点滚动 | 待执行 |
| TC-D03 | 任意 | 设置页切换 Typewriter | 与菜单状态保持同源同步 | 待执行 |
| TC-D04 | 手动关闭 Typewriter | 收起侧栏进入 Zen | Typewriter 被强制开启 | 待执行 |
| TC-D05 | 执行 D04 后 | 退出 Zen | 恢复进入 Zen 前偏好（关闭） | 待执行 |
| TC-D06 | 手动开启 Typewriter | 进入并退出 Zen | 退出后仍保持开启 | 待执行 |
| TC-D07 | 短文（<=1屏） | 开启 Typewriter 并输入 | 不出现强制滚动锁定 | 待执行 |
| TC-D08 | 长文 + 中文输入法 | 连续中文输入含候选 | 无明显抖动/回跳 | 待执行 |
| TC-D09 | 长文 | ↑/↓ 方向键移动光标 | 光标行仍趋近 45% 锚点 | 待执行 |
| TC-D10 | 长文文末 | 持续输入到文末 | 光标不贴底，40vh 保持有效 | 待执行 |
| TC-H02 | Min + 长文 + Typewriter | 展开/收起 Overlay 后继续输入 | Typewriter 行为持续有效 | 待执行 |
| TC-H04 | Typewriter 手动关闭 | 进入 Zen -> Focus -> 退出 | 最终恢复到手动关闭 | 待执行 |
