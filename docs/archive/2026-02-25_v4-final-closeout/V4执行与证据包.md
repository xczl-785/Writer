# V4 执行与证据包

- 状态：执行中
- 最后更新时间：2026-02-24
- 关联计划：`docs/current/V4重构实施计划.md`

## 1. Baseline（执行前必填）

| 项目                | 当前值 | 命令/来源                            |
| ------------------- | ------ | ------------------------------------ |
| Editor.tsx 行数     | 290    | `wc -l src/ui/editor/Editor.tsx`     |
| `src/state/actions` | 已创建 | `src/state/actions/*`                |
| ErrorService        | 已创建 | `src/services/error/ErrorService.ts` |
| editor config       | 已创建 | `src/config/editor.ts`               |

## 2. 任务执行清单

### WP-V4-A Editor 模块化（FR-V4-01）

- [x] A-01 建立 `components/` 目录（`hooks/` 待后续）
- [x] A-02 工具栏渲染与命令逻辑拆分（常量与命令定义已拆分）
- [x] A-03 查找替换面板拆分
- [x] A-04 表格插入弹层拆分
- [x] A-05 `Editor.tsx` 主容器收敛（当前 290 行，已达门禁）

### WP-V4-B 常量配置化（FR-V4-04）

- [x] B-01 新增 `src/config/editor.ts`
- [x] B-02 迁移查找替换、表格、状态提示硬编码（第一批）
- [x] B-03 清理重复常量（图片允许 MIME 类型迁移至 `EDITOR_CONFIG.image.allowedMimeTypes`）

### WP-V4-C Action 层统一（FR-V4-02）

- [x] C-01 新增 `src/state/actions/*`
- [x] C-02 迁移 Sidebar 跨 store 协调逻辑（第一批）
- [x] C-03 迁移 WorkspaceManager 跨 store 协调逻辑（第一批）

### WP-V4-D 错误处理统一（FR-V4-03）

- [x] D-01 新增 `src/services/error/ErrorService.ts`
- [x] D-02 替换关键路径 `console.error` 直出（WorkspaceManager）
- [x] D-03 对齐状态栏错误提示格式（图片粘贴错误统一为 `Failed to ...`；关键路径统一通过 `ErrorService`）

## 3. V4-GAP（遗漏补充登记）

| ID                     | 来源                                       | 是否阻塞 V4 主线 | 处理方式                        | 证据 |
| ---------------------- | ------------------------------------------ | ---------------- | ------------------------------- | ---- |
| V4-GAP-01 (CRLF)       | `V3遗留与V4待办.md` 全项目 CRLF 行尾不一致 | 是               | 纳入 V4 启动修复（工程基线）    |      |
| V4-GAP-02 (BUG-06)     | `V3遗留与V4待办.md` 外部粘贴误触发代码样式 | 是               | 纳入 V4（与编辑器重构并行修复） |      |
| V4-GAP-03 (Tauri 补测) | `V3遗留与V4待办.md` 原生调用补测           | 否（本版不做）   | 推迟，V4 重构版本不纳入         |      |
| V4-GAP-04 (BUG-01)     | `V3遗留与V4待办.md` 表格跨区域选中困难     | 否（已完成）     | 关闭，V4 不再处理               |      |
| V4-GAP-05 (BUG-07)     | `V3遗留与V4待办.md` Backspace 删表特效缺失 | 否（已完成）     | 关闭，V4 不再处理               |      |
| V4-GAP-06 (REQ-V3-05)  | `V3遗留与V4待办.md` 结构感知 PoC           | 否（本版不做）   | 推迟到 V5，V4 不纳入            |      |

## 4. QA 与门禁记录

| 检查项      | 命令                             | 结果    | 证据                             |
| ----------- | -------------------------------- | ------- | -------------------------------- |
| 单元测试    | `npm run test`                   | PASS    | 本地执行通过（98 tests）         |
| 构建        | `npm run build`                  | PASS    | 本地执行通过                     |
| Lint        | `npm run lint`                   | PASS    | 本地执行通过（无 error/warning） |
| 覆盖率      | `npm run test -- --coverage`     | BLOCKED | 缺少 `@vitest/coverage-v8` 依赖  |
| Editor 行数 | `wc -l src/ui/editor/Editor.tsx` | PASS    | `290`（目标 `< 300`）            |

本轮补充：

1. 完成 `V3遗留与V4待办` 的状态回填（已完成/推迟到 V5）。
2. 完成第一批常量配置化：find/replace、表格默认值、状态提示超时、图片大小上限。
3. 完成第一批 Editor 组件拆分：`FindReplacePanel`、`InsertTablePopover`，并抽离 `constants.ts`。
4. 完成第二批 Editor 主容器收敛：新增 `EditorShell`、`useTransientStatus`、`useInsertTable`、`useFindReplace`、`useToolbarCommands`、`editorExtensions` 并完成回归验证。
5. 完成错误处理收敛：新增 `ErrorService.log`，替换关键 `console.error` 调用点。
6. 完成状态栏错误格式对齐：图片粘贴失败状态统一为 `Failed to ...` 风格。
7. 完成常量去重：`useImagePaste` 的允许图片类型迁移到 `src/config/editor.ts`。

## 5. Go/No-Go 审计结论

- 结论：`NO-GO`（仅因覆盖率门禁未能执行）
- 日期：2026-02-24
- 审计依据：`docs/current/V4需求主文档.md` 第 3 章门禁

补充说明：

1. 代码门禁已全部通过（test/build/lint + Editor 行数门禁）。
2. 覆盖率门禁被环境依赖阻塞（未安装 `@vitest/coverage-v8`）。
3. 安装覆盖率依赖并通过门禁后，可直接切换为 `GO` 并进入归档。

## 6. 归档索引

| 证据类型   | 路径                             |
| ---------- | -------------------------------- |
| 需求主文档 | `docs/current/V4需求主文档.md`   |
| 实施计划   | `docs/current/V4重构实施计划.md` |
| 追溯矩阵   | `docs/current/V4需求追溯矩阵.md` |
| 启动看板   | `docs/current/V4启动看板.md`     |
