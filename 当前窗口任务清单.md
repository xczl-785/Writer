# 当前窗口任务清单

> 更新日期：2026-03-13  
> 用途：在当前会话中保留明确执行顺序，避免上下文压缩后遗漏。

## 1. 当前窗口目标

完成菜单系统的第一批整改，范围包括：

- 修复真实缺失项
- 将暂不支持项改为置灰
- 不在当前窗口内扩展为新的大需求

## 2. 当前窗口计划清单

### 2.1 第一阶段：置灰优先

- [x] 将 `File > Save As` 改为置灰
- [x] 将 `File > Export PDF` 改为置灰
- [x] 将 `File > Export HTML` 改为置灰
- [x] 将 `File > Export Image` 改为置灰
- [x] 将 `View > Source Mode` 改为置灰

### 2.2 第二阶段：修复高优先级遗漏

- [x] 修复 `任务列表`
- [x] 补齐 `Format > Link`
- [x] 补齐 `Format > Image`
- [x] 补齐 `Format > Highlight`
- [x] 补齐 `Format > Underline`

### 2.3 第三阶段：一致性整改

- [x] 将 Slash 菜单图片插入链路对齐到现有图片系统

## 3. 不在当前窗口内实现

以下内容只保留标记，不在本窗口直接开发：

- [ ] `Paragraph > Math Block`：单独需求开发
- [ ] `View > Source Mode` 完整能力：单独需求开发
- [ ] `Save As` 完整能力：单独需求开发
- [ ] `Export PDF / HTML / Image` 完整能力：单独需求开发

## 4. 当前建议起点

下一步直接开始：

1. 回归验证菜单行为
2. 视需要整理提交
